# 英语绘本故事馆：儿童体验改进与验收

版本：story-child-v1，2026-09-22。保留 `/english-story.html` 网址及其他工具页面。

## 自评方法与结论

以下是基于代码审查、浏览器行为测试和截图复查的工程自评，不是儿童实测、独立评级或发音认证。8项等权，每项10分，平均 **8.0 / 10**，超过本次7分门槛。未把浏览器脚本通过当作儿童一定喜欢的证据。

| 维度 | 分数 | 依据及扣分边界 |
|---|---:|---|
| 入口和返回 | 9 | 书架、继续阅读、固定名称的返回入口；不再让选书面板消失后无法返回 |
| 字号、图片与触控 | 8 | 大字号单句、圆润按钮、主要触控区至少48px；封面放大、去掉挡脸标签；插图仍是简化矢量角色，不是逐页精绘 |
| 学习活动与反馈 | 8 | 全句播放结束才记录听读，附图片和序号的理解题，三次错误暂停提示；每故事仅1道理解题，不能代表全面掌握 |
| 朗读与容错 | 7 | 保持整句自然音高，3档语速、失败不跳过、取消旧队列、不用中文声音兜底；仍依赖设备英语语音，未认证所有设备发音 |
| 隐私与反馈诚实性 | 9 | 移除按时长伪造的词句分、摄像头请求和7分钟保存门槛；麦克风明确勾选、只在本机、随时停止；勾选不是身份验证 |
| 学习负担 | 8 | 一次一句、中文按需显示、允许分次阅读、10分钟前台使用后休息提醒；不是跨设备或防篡改的家长控制系统 |
| 容错与兼容 | 8 | 7种横竖屏尺寸、全部故事句子、进度恢复、无语音/权限拒绝/存储失败等；尚未在实体iPad、安卓手机和真实孩子中试用 |
| 加载与资源 | 7 | 无第三方脚本/图片/字体下载，删除大量旧录音与假评分代码；资源体积下降，但拆分资源令模拟首访略慢约0.1秒，仍约1.06秒可选书 |

## 核心修复

- 6个故事以图文书架呈现；每次只显示一句正文，保留上一句、下一句、再听和连续听。
- 中文只在点击后展示，不混入英文朗读；场景按剧情节点而非平均句数切换。
- 播放错误、隐藏页面、切换故事和快速点击会取消旧语音队列，不误记已听完。
- 阅读进度保存在独立本地键 `englishStoryChildV1`；原 `englishStoryStats` 保留，不把旧时长统计转换成掌握成绩。
- 可选短复述，最多录2分钟。只请求音频；权限取消后即使授权晚返回也立即释放麦克风。下载扩展名跟随实际MIME，不再把webm伪装为wav。
- 回放由孩子与家长自行判断；不存在关键词识别、发音评分或“讲得好才允许保存”的伪功能。

## 测试

`tools/story_acceptance.py`：6个故事、91句（由数据动态枚举）在320、375、390、768、844横屏、1024、1440视口测试；返回、真实语音结束门槛（模拟事件）、语音错误、翻译、恢复进度、三次错误提示、麦克风同意/取消、下载MIME。无页面JavaScript错误。

`tools/story_safety.py`：权限拒绝、切后台关闭麦克风、10分钟提醒及60秒返回、存储不可用时继续阅读。

语音与录音设备在自动化中使用模拟对象，验证的是逻辑，不能据此宣称真人发音或硬件音质合格。公开网站部署后另行运行同一验收。

## 本地移动网络模拟

390×844、1.6Mbps下载、150ms延迟、4倍CPU降速，全新浏览器上下文，每版3次；不是所有用户的实际加载速度。

| 指标 | 原版 | 新版 |
|---|---:|---:|
| 可选书时间 | 968 / 958 / 948ms | 1063 / 1058 / 1044ms |
| 中位数 | 958ms | 1058ms |
| 首次文档和资源传输 | 约115KB | 约74KB |

新资源可独立使用浏览器缓存，但没有新增Service Worker，因此不承诺首次离线可用。测试工具 `story_metrics.py` 使用从改动前Git提交提取的临时baseline页面；截图与运行输出在被忽略的 `.visual-story/` 中。

## 内容与发行边界

沿用站内原有6个小猪小象主题改写故事和原有SVG角色，没有复制新的出版绘本正文、图片或音频。页面明确标注不是出版绘本原文、未声明出版社授权；理解题只针对本页版本。此优化不是商业发行许可审查，不能据此认定这些人物/改写素材可直接售卖。

## 建议下一轮真实验证

请3—5位目标年龄孩子各独立完成“选书—听3句—翻译—答题—返回”，观察是否需家长提示；在真实iOS/安卓设备上人工试听全部句子。优先据此调整，而非单纯追求更高自评分。
